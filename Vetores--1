#include <iostream>

using namespace std;

void vetler(int* vetx, int tm)
{
		for(int i=0; i<tm; i++)
		{
			cin>> vetx[i];
		}

} 

void vetimp(int* vetler, int tmlr)
{
		for(int i=0; i<tmlr; i++)
		{
			cout << vetler[i]<< endl;
		
		}
}



int main()
{
	int tmvet;
	cin>>tmvet;
	int vetA[tmvet],vetB[tmvet];
	vetler(vetA,tmvet);
	vetler(vetB,tmvet);
	
	vetimp(vetA,tmvet);
	vetimp(vetB,tmvet);
	

}
